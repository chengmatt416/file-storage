{
  "name": "github-file-share",
  "version": "1.0.0",
  "description": "A file sharing website using GitHub as storage",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "npm run copy-public",
    "copy-public": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "@octokit/rest": "^18.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "multer": "^1.4.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": ">=14"
  }
}